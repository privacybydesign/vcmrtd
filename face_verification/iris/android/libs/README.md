# Iris SDK binary

Place the vendor-provided `iris.aar` in this directory as `iris.aar`.

It's gitignored (see `../../.gitignore`) because it's a proprietary binary
shared privately by passportreader.app, not something this public repository
redistributes. Get your own copy from the vendor and drop it here before
building anything that depends on this package.
