name: face_verification
description: Face verification and liveness detection — standalone package for use with vcmrtd documents

version: 1.0.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.12.0
  image: ^4.5.4
  flutter_litert: ^3.2.0
  native_toolchain_c: ^0.19.0
  logging: ^1.3.0
  meta: ^1.15.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  test: ^1.16.5
  lints: ^6.0.0

flutter:
  assets:
    - lib/src/models/face_detector.tflite
    - lib/src/models/face_landmarks_detector.tflite
    - lib/src/models/face_blendshapes.tflite
    - lib/src/models/minifasnet_v1se.tflite
    - lib/src/models/minifasnet_v2.tflite
    - lib/src/models/bigsmall_1.tflite
    - lib/src/models/bigsmall_2.tflite
    - lib/src/models/bigsmall_3.tflite
    - lib/src/models/GhostFaceNet_fp32_V2.tflite
